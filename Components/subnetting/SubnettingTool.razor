@inject I18n i18n
@using global::subnetting

<div class="tool">
    <div class="input-row">
        <Subnetting.Components.form.ip.IpInput Bns=@subnetResolver.ip?.bns />
        <Subnetting.Components.form.ip.MaskInput Bns=@subnetResolver.mask?.bns />
    </div>
    <div>
        <RadzenButton Text=@i18n.T("components.subnet-tool.actions.subnet.Text") />
    </div>
</div>

@code {
    SubnetResolver subnetResolver = new SubnetResolver();
}