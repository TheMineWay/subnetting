@inject I18n i18n;

<Subnetting.Components.form.bytes_network_structure.BytesNetworkStructureInput
    label=@i18n.T("components.form.ip-input.Label") @bind-Value=@Bns IsValid=@IsValid />

@code {
    [Parameter]
    public BytesNetworkStructure? Bns { get; set; }

    [Parameter]
    public Action<BytesNetworkStructure>? OnChange { get; set; }

    [Parameter]
    public bool? IsValid { get; set; }
}